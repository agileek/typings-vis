{
  "name": "typings-vis",
  "version": "0.0.0",
  "description": "A type definition for the visjs library.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/agileek/typings-vis.git"
  },
  "scripts": {
    "lint": "node_modules/.bin/tslint vs.d.ts",
    "compile": "node_modules/.bin/tsc vs.d.ts",
    "test": "npm run compile && npm run lint"
  },
  "devDependencies": {
    "tslint": "^4.0.2",
    "typescript": "^2.0.10"
  }
}
